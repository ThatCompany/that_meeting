<%= l(:text_attendee_invited, :id => "##{@issue.id}", :author => @author) %>

----------------------------------------
<%= render :partial => 'issue', :formats => [ :text ], :locals => { :issue => @issue, :user => @user, :users => @users, :issue_url => @issue_url } %>
